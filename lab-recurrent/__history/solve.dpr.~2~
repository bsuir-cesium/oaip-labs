program solve;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var
  I, N: Integer;
  arr: array of Integer;

begin
  write('¬ведите число: ');
  readln(N);
  setLength(arr, N);
  Randomize;
  for I := 0 to N - 1 do
  begin
    arr[I] := Random(100);
    Write(arr[i], ' ');
  end;
  writeln;

end.
